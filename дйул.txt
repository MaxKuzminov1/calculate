sudo sysctl -a # Просмотр текущих параметров
# Увеличение количества файловых дескрипторов (полезно для серверов)
echo "fs.file-max = 500000" | sudo tee -a /etc/sysctl.conf 
# Оптимизация сетевых параметров (для ускорения интернета)
echo "net.core.rmem_max = 16777216" | sudo tee -a /etc/sysctl.conf 
echo "net.ipv4.tcp_fin_timeout = 15" | sudo tee -a /etc/sysctl.conf

# Применение изменений
sudo sysctl -p


ip a
ip route show default
sudo ethtool eth0Э(свой) | grep "Link detected"

ping google.com

Установлено базовое программное обеспечение
#1. Обновление пакетов
sudo apt update && sudo apt upgrade -y
#2. Установка архиватора (7-zip)
sudo apt install p7zip-full
7z  # должен вывести справку
#3. утилиты для мониторинга системы:
sudo apt install htop
htop
sudo apt install hardinfo
hardinfo
#4. Установка офисного пакета(если не установили при установке ubuntu)
sudo apt install libreoffice
libreoffice –version
#5. Установите браузер (Firefox/Chromium)
sudo apt install firefox
sudo apt install chromium-browser
#6. Установите графический редактор (GIMP)
sudo apt install gimp
gimp –version
#7. Проверка списка установленного ПО 
apt list --installed


sudo apt install cups-pdf
sudo /etc/init.d/cups restart
sudo lpstat -p -d

sudo tar cvpzf backup.tgz --exclude=/proc --exclude=/lost+found --exclude=/backup.tgz --exclude=/mnt --exclude=/sys --exclude=/web /


sudo fdisk -l
#ищем что-то похожее на /dev/sda
sudo mkdir /mnt/backup
sudo mount /dev/sda /mnt/backup
sudo dd if=/dev/sda2 of=/mnt/backup/linux_backup.img bs=4M status=progress
gzip /mnt/backup/linux_backup.img
# убедитесь, что файл создан
ls -lh /mnt/backup/
  
# отмонтируйте диск
sudo umount /mnt/backup 

Созданы точки восстановления системы подразумеваются для ос Windows и MacOs. В Linux это не предусмотрено, а предусмотрено резервное копи-рование.


Созданы группы пользователей
Sudo apt install gnome-system-tools
 
сat /var/lod/auth.log или tail – f /var/log/auth.log #журнал аутентификации
last #последние попытки входа
tail -f /var/log/syslog #логи в реальном времени
